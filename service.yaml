apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: LoadBalancer  # Change the service type to LoadBalancer
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 80         # The port that will be exposed
      targetPort: 8080  # The port on the container that the service will forward to
